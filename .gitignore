# Секрети (НЕ комітити) — Supabase service_role та інші ключі
.supabase-service-key
.env
.env.local
*.key

# Фото товарів, підготовлені до завантаження в Supabase Storage (не в git)
_photos/

# Таблиці постачальника (приватні дані, швидко застарівають) — качати перед
# кожним запуском scripts/recalc_*.py, див. коментарі в самих скриптах
scripts/sup*.xlsx
scripts/sup*.csv

# macOS
.DS_Store
.vercel
.env*

# graphify — локальний кеш knowledge graph, регенерується /graphify
graphify-out/

# Скріншоти й артефакти локальних інструментів (Playwright MCP тощо) — не в git,
# інакше роздувають кожен деплой Vercel
.playwright-mcp/
artifact-*.png

# калькулятор прибутку — окремий Vercel-проект (DEPLOY-PROFIT-CALC.md), НЕ частина цього репо
profit-calc/
