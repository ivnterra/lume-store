# План: інтеграція LP-CRM з Новою Поштою (ТТН + статуси посилок)

Мета: оператор колл-центру обробляє лід у LP-CRM, **створює ТТН прямо з картки
замовлення**, номер ТТН автоматично зберігається у замовленні, а статуси посилки
(в дорозі, прибула, отримана, повернення) **автоматично підтягуються** у CRM.

## Ключова рекомендація (архітектура)
ТТН треба створювати **всередині LP-CRM**, а не в окремому кабінеті Нової Пошти.
LP-CRM має вбудовану інтеграцію з API НП: коли оператор оформлює ТТН у замовленні,
CRM сама звертається до НП, отримує номер ТТН і далі періодично опитує статус.
Це і є те, що потрібно — окрема синхронізація не нужна.

⚠️ Якщо колл-центр зараз робить ТТН у власному кабінеті НП (поза CRM) — статуси в
CRM не з'являться. Тоді є два шляхи: (А, рекомендовано) перевести операторів на
створення ТТН із CRM; (Б) налаштувати зворотній імпорт ТТН за номером — складніше і
менш надійно.

## Що потрібно ДО налаштування (підготовка)
1. **Бізнес-кабінет Нової Пошти** (https://new.novaposhta.ua) — зареєстрований на
   ФОП/компанію, з якого відправляєте.
2. **API-ключ НП**: кабінет НП → Налаштування → Безпека → «Ключі API» → створити
   ключ. Це робить власник кабінету (ключ — секрет, нікому не передавати у чаті).
3. **Дані відправника**: контрагент-відправник, контактна особа, телефон, місто та
   відділення/адреса відправлення (звідки шлете посилки).

## Налаштування в LP-CRM (покроково)
1. **Служби доставки** (`/deliverys`) — «Нова Пошта» вже є (id 1). Переконатися, що
   увімкнена.
2. **Накладні** (`/shipment`) → кнопка **«Налаштування»** → розділ **«Нова Пошта»**:
   - вставити **API-ключ** НП;
   - вибрати/створити **відправника** (контрагент, контактна особа, телефон);
   - вказати **місто та відділення відправлення** за замовчуванням;
   - зберегти.
3. **Оплата** (`/payments`) — для накладеного платежу використовується «Післяплата»
   (id 4). Перевірити, що для післяплати у ТТН передається сума до сплати (= сума
   замовлення).
4. **Тест**: взяти тестове замовлення → у картці створити ТТН → переконатися, що
   номер ТТН з'явився і статус почав оновлюватися.

## Робочий процес колл-центру (після налаштування)
1. Лід падає у CRM зі статусом «Новий» (це вже працює — інтеграція сайту готова).
2. Оператор телефонує, підтверджує, заповнює **отримувача**: ПІБ, телефон, місто,
   відділення НП (поле у нас уже приходить з сайту в коментарі/адресі).
3. Натискає **«Створити ТТН»** у картці → CRM звертається до НП → номер ТТН
   зберігається у замовленні.
4. Статус посилки далі оновлюється автоматично і видно у списку замовлень.

## Автостатуси (рекомендовано налаштувати)
У `/statusy` (Статуси замовлень) + правила:
- ТТН створено → автоматично статус **«Відправлено»**;
- НП повертає «Прибув у відділення» → можна тригерити **SMS клієнту** з номером ТТН;
- НП повертає «Отримано» → статус **«Виконано»**;
- НП повертає «Відмова/повернення» → статус **«Повернення»**.
SMS-шаблони — у `/template_sms`; авто-SMS з номером ТТН при відправленні економить
дзвінки і зменшує невикупи.

## Реєстри (щоденне передавання)
`/registers_novaposhta` — наприкінці дня формуєте реєстр зі створених ТТН і
передаєте посилки у відділення/кур'єру одним документом. Зменшує ручну роботу.

## Рекомендації та підводні камені
- **Єдине джерело ТТН** — лише CRM. Не змішувати з ручним кабінетом НП, інакше
  статуси «розповзуться».
- **Перевірити ліміт/тариф** API НП (зазвичай безкоштовно для відправника).
- **Післяплата**: переконатися, що сума накладеного платежу і контроль оплати
  передаються коректно (комісія НП — на відправника чи отримувача).
- **Адреса/відділення**: бажано підказку відділень НП підтягувати у формі сайту
  (автокомпліт), щоб оператор не виправляв вручну — можу додати окремо.
- **Доступи операторів**: у `/user_groups` обмежити, хто може створювати/скасовувати
  ТТН.
- **Почати з тесту** на 2–3 реальних замовленнях, далі масштабувати.

## Що з цього можу зробити я
- Допомогти ввести налаштування НП у CRM через браузер (ключ вводите ви).
- Налаштувати автостатуси і SMS-шаблон з номером ТТН.
- Додати у форму сайту автокомпліт міст/відділень Нової Пошти (зменшить помилки).
