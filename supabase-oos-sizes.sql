-- ============================================================
--  LUMÉ — поле для розмірів, яких тимчасово немає в наявності
--  (наявність + резерв < 20 у постачальника).
--  Витрина рендерить такі розміри перекресленими/неактивними.
--  Виконати один раз у Supabase → SQL Editor.
-- ============================================================

alter table public.products
  add column if not exists oos_sizes text[] not null default '{}';
