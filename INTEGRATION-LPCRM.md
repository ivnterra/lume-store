# Інтеграція сайту LUMÉ з LP-CRM

Замовлення та заявки з сайту автоматично потрапляють у CRM-кабінет
**rosydial.lp-crm.biz** (окрім Telegram і Supabase).

## Як це працює
1. `index.html` — у позиціях замовлення передається `sku` і `price` кожного товару.
2. `api/order.js` — після збереження у Supabase викликає `sendToLpCrm()`.
3. `api/lpcrm.js` — мапить `sku → product_id` CRM, формує PHP-`serialize` масив
   товарів і надсилає POST на `https://rosydial.lp-crm.biz/api/addNewOrder.html`.
4. Помилка CRM не зриває замовлення (воно все одно йде у Telegram/Supabase).

## ⚠️ Що треба зробити вручну (один раз)
У Vercel → Project Settings → Environment Variables додати:

    LPCRM_KEY = 600a4f1c237803b0e9d5ee978a25bfd4

Потім **Redeploy**. Без цієї змінної інтеграція тихо вимкнена.
Ключ навмисно НЕ зашитий у код.

## Довідники CRM (ID)
- **Відділи (office):** 1 Київ · 2 Харків · 3 Одеса  → за замовч. `1`
- **Доставка (delivery):** 1 Нова Пошта · 2 Самовивіз · 3 Укрпошта  → за замовч. `1`
- **Оплата (payment):** 2 Передоплата · 4 Післяплата · 7 Передоплата на картку
  - «Передплата за реквізитами» → 2
  - «Післяплата» → 4
- **Категорії товарів:** 5 Купальники · 6 Пляжні накидки
- **Службовий товар** для заявок з форми зв'язку: `ZAYAVKA` (id 62)

## Каталог у CRM
Усі 63 товари сайту заведені у CRM (id 5–70, з розривами). Маппінг `sku → product_id`
знаходиться у `api/lpcrm.js` (об'єкт `SKU_TO_ID`).
Якщо додаєте новий товар — заведіть його у CRM і допишіть пару `'sku':'id'`.

⚠️ Синхронізації товарів між сайтом і CRM НЕМАЄ — це ручний список у коді,
його ніхто й ніщо не оновлює автоматично.

## Каталог оновлено (14.07.2026)
Товари `50/37`, `50/23`, `50/31` знайдені в CRM (id 70, 69, 68) і дописані
в `SKU_TO_ID` (`api/lpcrm.js`). Наразі всі товари сайту замаплені.

## Розміри
У CRM-товарах розмірної сітки немає, тому розмір кожної позиції передається
в полі `comment` замовлення (разом з артикулом, кількістю і сумою).

## Тестові замовлення
Під час налаштування створено 2 тестові замовлення (ім'я «ТЕСТ … Інтеграція LUMÉ»,
внутрішні id 2 і 3 у CRM) — їх можна видалити в розділі «Замовлення».
